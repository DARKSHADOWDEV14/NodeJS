npm init -y

// inicializacion del proyecto para que poder correr
npm install express -E
import express from express
const app = express()

// programa para ver detalles de lo que se hace en la app
npm install morgan
app.use(logger('dev'))

//
npm install socket.i -E
import { createServer} from ''node:http'

